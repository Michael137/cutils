# Build instructions:

# Clean src/target/containers directory:
make clean

# Compile container source files without linking:
make compile_src

# Build tests:
make compile_tests

# Make everything
make all

# Run tests
make test
